sudo: required
language: python
python:
- "2.7"
services:
  - docker
before_install:
  - docker pull ros:melodic
  - docker run -d ros:melodic /bin/sh "source /opt/ros/melodic/setup.bash"
install:
  - pip install -r requirements.txt
script: ./run_tests.sh
notifications:
  email:
    on_success: never
    on_failure: always
